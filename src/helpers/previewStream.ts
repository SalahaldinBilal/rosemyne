// Removed: the WebSocket preview stream was replaced by the in-page canvas
// renderer (src/helpers/canvasRenderer.ts). See EFFECT_PREVIEW_PLAN.md for how
// to restore it. This file can be deleted.
export {};
